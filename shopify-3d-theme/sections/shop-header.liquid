<!-- SHOP HEADER (homepage only, inside #shop-content) -->
<header class="shop-header" role="banner">
  <a href="/" class="shop-logo" aria-label="{{ shop.name }} Home">
    {{ section.settings.logo_first }}<span>{{ section.settings.logo_second }}</span>
  </a>

  <nav aria-label="Main navigation">
    <ul class="shop-nav">
      {% for link in linklists[section.settings.menu].links %}
        <li>
          <a href="{{ link.url }}">{{ link.title }}</a>
        </li>
      {% endfor %}
    </ul>
  </nav>

  <div class="shop-nav-actions">
    {% if shop.customer_accounts_enabled %}
      <a href="{{ routes.account_url }}" class="nav-icon-btn" aria-label="Account">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/>
        </svg>
      </a>
    {% endif %}
    <a href="{{ routes.search_url }}" class="nav-icon-btn" aria-label="Search">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <circle cx="11" cy="11" r="7"/><path d="M21 21l-4.35-4.35"/>
      </svg>
    </a>
    <button class="nav-icon-btn" aria-label="Open cart" data-cart-open>
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/>
        <path d="M16 10a4 4 0 01-8 0"/>
      </svg>
      <span data-cart-count style="display:none;"></span>
    </button>
  </div>
</header>

{% schema %}
{
  "name": "Shop Header",
  "settings": [
    {
      "type": "text",
      "id": "logo_first",
      "label": "Logo Text (first part)",
      "default": "ÉLAN"
    },
    {
      "type": "text",
      "id": "logo_second",
      "label": "Logo Text (accent part)",
      "default": "STUDIO"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Navigation Menu",
      "default": "main-menu"
    }
  ],
  "presets": [
    {
      "name": "Shop Header",
      "category": "Custom"
    }
  ]
}
{% endschema %}
